payload:
  # Локальные дополнения Android-пакетов для wl.yaml tun.exclude-package.
  # Базовый список: wld.list → фильтр ru-app-list; дополнения — только здесь (без ru-apps-custom).
  # Добавляй сюда пакеты, которые должны обходить TUN в режиме «белые списки», но не попали в автоматический матч.
